import header from './Header.module.scss';

export const Header = () => (
  <header className={header.ly_header}>
    <h1 className={header.el_app_title}>Task Management Clone App</h1>
  </header>
);
